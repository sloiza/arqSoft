<h1>Sistema de inscripción a materias</h1>
<p>Seleccione materia:</p>
{{#each cursos}}
        <p><a href="http://localhost:8080/new/{{ this.data.codigo }}">{{ this.data.codigo }} - {{ this.data.nombre }}</a></p>
{{/each}}